# Run mockup web
change working directory to `/web`
```
npm run start
```

# Run mockup web
change working directory to `/`
```
npx playwright test
```

Note: you need to run `npm install` before run program